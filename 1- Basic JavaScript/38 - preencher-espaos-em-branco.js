const myNoun = "dog";
const myAdjective = "big";
const myVerb = "ran";
const myAdverb = "quickly";

// Altere apenas o código abaixo desta linha
const wordBlanks = "Did you saw my " + myNoun + "? It's so " + myAdjective + " but " + myVerb + " so " + myAdverb + " from me..."; // Altere esta linha
// Altere apenas o código acima desta linha